name: Format and Lint

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  # JOB 1: FORMATAGE AUTOMATIQUE EN PREMIER - TOUJOURS
  auto-format:
    name: Auto-Format Code
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        fetch-depth: 0
    
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install Black and isort
      run: |
        pip install black isort
    
    - name: Format with Black (ALWAYS - NO CHECK)
      run: |
        echo "🔧 Formatage automatique des fichiers avec Black..."
        echo "📁 Répertoire de travail:"
        pwd
        echo "📁 Structure du dépôt:"
        ls -la
        echo "📁 Recherche des dossiers app/ et tests/..."
        # Déterminer les chemins corrects pour la structure monorepo
        if [ -d "backend/app" ] && [ -d "backend/tests" ]; then
          echo "✅ Structure monorepo détectée: backend/app/ et backend/tests/"
          APP_PATH="backend/app"
          TESTS_PATH="backend/tests"
        elif [ -d "app" ] && [ -d "tests" ]; then
          echo "✅ Structure dépôt backend détectée: app/ et tests/"
          APP_PATH="app"
          TESTS_PATH="tests"
        else
          echo "⚠️ Structure non reconnue, recherche approfondie..."
          find . -type d -name "app" | head -3
          find . -type d -name "tests" | head -3
          if [ -d "backend/app" ]; then
            APP_PATH="backend/app"
            TESTS_PATH="backend/tests"
          else
            echo "⚠️ Skip du formatage Black - dossiers app/ et tests/ non trouvés"
            exit 0
          fi
        fi
        echo "✅ Exécution de Black sur $APP_PATH et $TESTS_PATH..."
        black "$APP_PATH" "$TESTS_PATH" 2>&1 || echo "⚠️ Erreur Black (non-bloquant)"
    
    - name: Format with isort (ALWAYS - NO CHECK)
      run: |
        echo "🔧 Formatage automatique des imports avec isort..."
        # Déterminer les chemins corrects (même logique que Black)
        if [ -d "backend/app" ] && [ -d "backend/tests" ]; then
          APP_PATH="backend/app"
          TESTS_PATH="backend/tests"
        elif [ -d "app" ] && [ -d "tests" ]; then
          APP_PATH="app"
          TESTS_PATH="tests"
        else
          echo "⚠️ Skip du formatage isort - dossiers manquants"
          exit 0
        fi
        echo "✅ Exécution de isort sur $APP_PATH et $TESTS_PATH..."
        isort "$APP_PATH" "$TESTS_PATH" 2>&1 || echo "⚠️ Erreur isort (non-bloquant)"
    
    - name: Commit and push formatted code
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        # Déterminer les chemins corrects
        if [ -d "backend/app" ]; then
          APP_DIR="backend/app"
          TESTS_DIR="backend/tests"
        elif [ -d "app" ]; then
          APP_DIR="app"
          TESTS_DIR="tests"
        else
          APP_DIR=""
        fi
        if [ -n "$APP_DIR" ]; then
          # Vérifier les changements
          if [ -d "$TESTS_DIR" ]; then
            CHANGED=$(git status --porcelain "$APP_DIR" "$TESTS_DIR" 2>/dev/null)
          elif [ -d "backend/tests" ]; then
            CHANGED=$(git status --porcelain "$APP_DIR" "backend/tests" 2>/dev/null)
          elif [ -d "tests" ]; then
            CHANGED=$(git status --porcelain "$APP_DIR" "tests" 2>/dev/null)
          else
            CHANGED=$(git status --porcelain "$APP_DIR" 2>/dev/null)
          fi
          if [ -n "$CHANGED" ]; then
            echo "📝 Changements détectés, commit automatique..."
            if [ -d "$TESTS_DIR" ]; then
              git add "$APP_DIR" "$TESTS_DIR" || true
            elif [ -d "backend/tests" ]; then
              git add "$APP_DIR" "backend/tests" || true
            elif [ -d "tests" ]; then
              git add "$APP_DIR" "tests" || true
            else
              git add "$APP_DIR" || true
            fi
            git commit -m "style: Auto-format code with Black and isort [skip ci]" || exit 0
            git push || exit 0
            echo "✅ Fichiers formatés et poussés automatiquement"
          else
            echo "✅ Aucun changement nécessaire, tout est déjà formaté"
          fi
        else
          echo "⚠️ Les dossiers app/ ou tests/ n'existent pas, skip du commit"
        fi

  # JOB 2: LINTING (non-bloquant, après le formatage)
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    needs: auto-format  # Attendre que le formatage soit terminé
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-python@v5
      with:
        python-version: '3.12'
    
    - name: Install linting tools
      run: |
        pip install flake8 pylint mypy
    
    - name: Run Flake8 (non-blocking)
      continue-on-error: true
      run: |
        if [ -d "backend/app" ] && [ -d "backend/tests" ]; then
          flake8 backend/app/ backend/tests/ --max-line-length=120 --extend-ignore=E203,W503,E501 || true
        elif [ -d "app" ] && [ -d "tests" ]; then
          flake8 app/ tests/ --max-line-length=120 --extend-ignore=E203,W503,E501 || true
        else
          echo "⚠️ Les dossiers app/ ou tests/ n'existent pas, skip Flake8"
        fi
    
    - name: Run Pylint (non-blocking)
      continue-on-error: true
      run: |
        if [ -d "backend/app" ]; then
          pylint backend/app/ --disable=C0111,C0103,R0913,R0903,W0613 || true
        elif [ -d "app" ]; then
          pylint app/ --disable=C0111,C0103,R0913,R0903,W0613 || true
        else
          echo "⚠️ Le dossier app/ n'existe pas, skip Pylint"
        fi
    
    - name: Run MyPy (non-blocking)
      continue-on-error: true
      run: |
        if [ -d "backend/app" ]; then
          mypy backend/app/ --ignore-missing-imports --no-strict-optional || true
        elif [ -d "app" ]; then
          mypy app/ --ignore-missing-imports --no-strict-optional || true
        else
          echo "⚠️ Le dossier app/ n'existe pas, skip MyPy"
        fi
