[pytest]
# Configuration pytest pour le projet RAG Photographie

# Répertoires de tests
testpaths = tests

# Patterns de fichiers de test
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Options par défaut
addopts = 
    -v
    --tb=short
    --strict-markers
    --disable-warnings

# Marqueurs personnalisés
markers =
    slow: tests qui prennent du temps à exécuter
    integration: tests d'intégration
    unit: tests unitaires
    database: tests nécessitant une base de données
    api: tests de l'API
    auth: tests d'authentification
    security: tests de sécurité
    ocr: tests OCR
    rag: tests RAG

# Configuration de couverture (optionnel)
# [tool:pytest]
# coverage_source = app

